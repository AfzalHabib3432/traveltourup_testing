import { INPUT_FIELD_CLASS } from "@/components/ui/inputFieldStyles";

/** @deprecated Use `<Input />` defaults — kept for any legacy imports */
export const authInputClass = INPUT_FIELD_CLASS;

export const authPrimaryButtonClass =
  "flex w-full items-center justify-center gap-2 rounded-xl bg-primary py-3.5 text-sm font-semibold text-primary-foreground shadow-md transition hover:bg-primary-600 focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2";

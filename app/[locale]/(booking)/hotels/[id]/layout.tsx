import type { ReactNode } from "react";

/** Metadata is defined on `page.tsx` via `generateMetadata` (per-offer titles). */
export default function HotelDetailLayout({ children }: { children: ReactNode }) {
  return children;
}
